#!/usr/bin/env ruby

require 'richrc'

if ARGV.empty?
  Richrc::RailsLoader.new.run!
else
  # thor is not loaded when executing rails console
  require 'rubygems'
  gem 'thor'
  require 'thor'
  require 'richrc/cli'
  Richrc::CLI.start
end
